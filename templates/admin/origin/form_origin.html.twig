<div>
    {{ form_start(form) }}
    <div class="grid-cols-2 grid">
        <div>
            {{ form_row(form.name) }}
        </div>
    </div>
    <div class="flex justify-between w-2/5">
        <a class="btn" href="{{ path('admin_panel_origin') }}">Retour à la liste</a>
        {{ form_row(form.submit) }}
    </div>
    {{ form_end(form) }}
</div>